{
  "name": "excelwizpro-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "ExcelWizPro Taskpane Add-in (Frontend UI bundled with Vite)",
  "type": "module",

  "homepage": "https://excelwizpro.github.io/excelwizpro-frontend/",

  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "deploy": "npm run build && gh-pages -d dist"
  },

  "devDependencies": {
    "gh-pages": "^6.3.0",
    "rimraf": "^5.0.5",
    "vite": "^5.2.0"
  }
}
